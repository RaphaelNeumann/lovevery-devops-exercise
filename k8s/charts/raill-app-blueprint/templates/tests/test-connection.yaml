apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "raill-app-blueprint.fullname" . }}-test-connection"
  labels:
    {{- include "raill-app-blueprint.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "raill-app-blueprint.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
